server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esta linha é a magia que faz o React Router funcionar no Docker:
        try_files $uri $uri/ /index.html;
    }
}